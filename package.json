{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "dependencies": {
    "init": "^0.1.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "turbo": "^1.11.3",
    "vite": "^5.0.10"
  }
}
